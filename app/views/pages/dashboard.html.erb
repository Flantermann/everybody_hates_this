<div class="container">
  <h1 class="heading-40-lightblue space-mt-40">Your Dashboard</h1>
  <div class="row">
    <div class="col-6">
      <div class="mission-grid-dashboard">
        <div class="dashboard-grid">
          <div class="d-flex justify-content-start align-items-start">
            <% if @user.photo.attached? %>
              <%= cl_image_tag @user.photo.key, class:"avatar-large" %>
            <% else %>
              <%= image_tag "user_3.png", class:"avatar-large" %>
            <% end %>
            <div class="pl-3">
              <h4 class="user-mission-card-title text-start"> Nickname </h4>
                <p class="user-mission-card-title text-start"> <%= @user.nickname %> (<%= @user.age %>)</p>
              <h4 class="user-mission-card-title text-start"> Address </h4>
                <p class="text-start user-address"> <%= @user.address%> </p>
            </div>
          </div>
          </div>
            <div class="button-position">
              <div class="btn-pink-variant button space-mt-30">
                <%= link_to "Update", edit_user_registration_path, class: "dropdown-item" %>
              </div>
            </div>
        </div>
    <div class="col-6">
      <div class="dashboard-grid">

      </div>
    </div>
    </div>
  </div>
  <div class="row">
    <div class="col-sm">
      <div class="mission-grid-dashboard">
        <div class="dashboard-grid">
          <h2 class="heading-24-spaced"> About me </h2>
            <p class="light" >
            Short text about myself. I am nice, 
            but very lazy. But now I want to
            change for the better. For real this time.
            And to be successful, I definitely need a buddy!
            Can't wait for your messages.
            </p>
          <div class="button-position">
            <div class="btn-pink-variant button space-mt-30">
              <%= link_to "Update", edit_user_registration_path, class: "dropdown-item" %>
            </div>
          </div>
      </div>
    </div>

    </div>
    <div class="col-6">
      <div class="mission-grid-dashboard">
        <div class="dashboard-grid">
            <h2 class="heading-24-spaced"> Your current mission </h2>
            <% if @current_mission %>
            <p class="light"> <%= link_to @current_mission.title, mission_path(@current_mission), class:"link" %> </p>
            <% else %>
            <p> Nothing here yet, click on the button to start your mission </p>
              <div class="button-position">
                <div class="btn-pink-variant button space-mt-30">
                  <%= link_to "Start mission", new_mission_path, class: "dropdown-item" %>
                </div>
              </div>
            <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col">
      <div class="mission-grid-dashboard">
        <div class="dashboard-grid">
        <h2 class="heading-24-spaced"> Need professional help? </h2>
          <p class="light" >
          Sometimes, even with the greatest buddy in the world and the best intentions we get stuck....
          that’s totally normal and nothing to be ashamed of. Our professional coaches will help you reach 
          the next milestone or finish your mission. You can book an online coaching for yourself, or you and your buddy together.
          </p>
          <div class="button-position">
              <div class="btn-pink-variant button space-mt-50">
                <%= link_to "Explore", new_mission_path, class: "dropdown-item" %>
              </div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-6">
      <div class="mission-grid-dashboard">
        <div class="dashboard-grid">
            <h2 class="heading-24-spaced"> Accomplished mission </h2>
            <% if @accomplished_missions.first %>
            <%= link_to "Accomplished missions", missions_path(acc_missions: @accomplished_missions) %>
            <% else @accomplished_missions.length == 0 %>
            <p> Nothing here </p>
            <% end %>
        </div>
      </div>
    </div>
  </div>
</div>


<% content_for :meta_title, "EHT | Mission" %>

<div class="container">
  <% if @mission.user == current_user %>
    <%= render "show-own-mission" %>
  <% else %>
    <%= render "show-buddy-mission" %>
  <% end %>
</div>
